/*
 * DO NOT EDIT!
 * Generated by msgpack-pjsekai dump.cs compiler version 0.4.0.
 * source: dump.cs
 */

#include "Sekai_CustomProfile_GenerateStoryFavoriteData.h"
#include "msgpack-pjsekai-common.h"

#include <stdlib.h>
#include <string.h>

int Sekai_CustomProfile_GenerateStoryFavoriteData_pack(msgpack_packer *pk, const Sekai_CustomProfile_GenerateStoryFavoriteData *value) {
    if (!pk || !value) return -1;
    uint32_t count = 0;
    if (value->has_shareNo) count++;
    if (value->has_storyType) count++;
    if (value->has_storyId) count++;
    if (value->has_comment) count++;
    if (value->has_isSpoiler) count++;
    msgpack_pack_map(pk, count);
    if (value->has_shareNo) {
        msgpack_pack_int64(pk, 0);
        msgpack_pack_int64(pk, value->shareNo);
    }
    if (value->has_storyType) {
        msgpack_pack_int64(pk, 1);
        if (value->storyType) { size_t len = strlen(value->storyType); msgpack_pack_str(pk, len); msgpack_pack_str_body(pk, value->storyType, len); } else { msgpack_pack_nil(pk); }
    }
    if (value->has_storyId) {
        msgpack_pack_int64(pk, 2);
        msgpack_pack_int64(pk, value->storyId);
    }
    if (value->has_comment) {
        msgpack_pack_int64(pk, 3);
        if (value->comment) { size_t len = strlen(value->comment); msgpack_pack_str(pk, len); msgpack_pack_str_body(pk, value->comment, len); } else { msgpack_pack_nil(pk); }
    }
    if (value->has_isSpoiler) {
        msgpack_pack_int64(pk, 4);
        if (value->isSpoiler) msgpack_pack_true(pk); else msgpack_pack_false(pk);
    }
    return 0;
}

int Sekai_CustomProfile_GenerateStoryFavoriteData_unpack(const msgpack_object *obj, Sekai_CustomProfile_GenerateStoryFavoriteData *out) {
    if (!obj || !out || obj->type != MSGPACK_OBJECT_MAP) return -1;
    memset(out, 0, sizeof(*out));
    for (uint32_t i = 0; i < obj->via.map.size; ++i) {
        const msgpack_object *key = &obj->via.map.ptr[i].key;
        const msgpack_object *val = &obj->via.map.ptr[i].val;
        if (mpj_key_eq_int(key, 0)) {
            if (val->type == MSGPACK_OBJECT_POSITIVE_INTEGER) { out->shareNo = (int32_t)val->via.u64; out->has_shareNo = true; }
            else if (val->type == MSGPACK_OBJECT_NEGATIVE_INTEGER) { out->shareNo = (int32_t)val->via.i64; out->has_shareNo = true; }
        }
        else if (mpj_key_eq_int(key, 1)) {
            if (val->type == MSGPACK_OBJECT_NIL) { out->storyType = NULL; out->has_storyType = true; }
            else if (val->type == MSGPACK_OBJECT_STR) { out->storyType = mpj_copy_msgpack_str(val); out->has_storyType = (out->storyType != NULL); }
        }
        else if (mpj_key_eq_int(key, 2)) {
            if (val->type == MSGPACK_OBJECT_POSITIVE_INTEGER) { out->storyId = (int32_t)val->via.u64; out->has_storyId = true; }
            else if (val->type == MSGPACK_OBJECT_NEGATIVE_INTEGER) { out->storyId = (int32_t)val->via.i64; out->has_storyId = true; }
        }
        else if (mpj_key_eq_int(key, 3)) {
            if (val->type == MSGPACK_OBJECT_NIL) { out->comment = NULL; out->has_comment = true; }
            else if (val->type == MSGPACK_OBJECT_STR) { out->comment = mpj_copy_msgpack_str(val); out->has_comment = (out->comment != NULL); }
        }
        else if (mpj_key_eq_int(key, 4)) {
            if (val->type == MSGPACK_OBJECT_BOOLEAN) { out->isSpoiler = val->via.boolean; out->has_isSpoiler = true; }
        }
    }
    return 0;
}

void Sekai_CustomProfile_GenerateStoryFavoriteData_free(Sekai_CustomProfile_GenerateStoryFavoriteData *value) {
    if (!value) return;
    free(value->storyType);
    value->storyType = NULL;
    value->has_storyType = false;
    free(value->comment);
    value->comment = NULL;
    value->has_comment = false;
}
